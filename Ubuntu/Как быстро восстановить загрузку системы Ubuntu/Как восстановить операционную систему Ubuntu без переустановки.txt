# –ассмотрим попул€рные и действенные методы восстановлени€ операционной системы Ubuntu. 
# ≈сли вы столкнулись с тем, что ваша система не запускаетс€ после обновлени€, 
# установки драйверов или по каким-нибудь другим причинам, то вам стоит воспользоватьс€ данной инструкцией.
#Ќа экране входа в систему воспользуйтесь комбинацией клавиш CTRL + ALT + F1 дл€ переключени€ в виртуальную консоль tty1, 
# возможно вам придетс€ переключитс€ на другую виртуальную консоль, в таком случае воспользуйтесь этой же комбинацией клавиш, но с F2 или F3 и т.д. 
# » затем выполните следующие команды:

sudo rm /var/lib/apt/lists/lock

sudo rm /var/lib/dpkg/lock

sudo rm /var/lib/dpkg/lock-frontend

sudo dpkg --configure -a

sudo apt clean

sudo apt update --fix-missing

sudo apt install -f

sudo dpkg --configure -a

sudo apt update

sudo apt dist-upgrade

# ≈сли причиной того, что система не загружаетс€ стала установка видеорайвера Nvidia, то дл€ решени€ проблемы его нужно удалить:

sudo apt remove --purge nvidia*

# » обновить систему:

sudo apt update && sudo apt dist-upgrade

# » перезагрузить систему:

sudo reboot

# “ак же может помочь переустановка рабочего окружени€. ƒл€ Ubuntu это будет следующа€ команда (не вли€ет на пользовательские данные):

sudo apt install --reinstall ubuntu-desktop