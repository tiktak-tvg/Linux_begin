#### Docker Сontainer
##### ПРОВЕРЯЕМ ВЕРСИЮ
```python
docker version
```
##### ПРОВЕРЯЕМ КОНФИГ
```python
docker info
```
##### ПРОВЕРЯЕМ REGISTRY
```python
→ docker run - -rm hello-world
→ docker container run - -rm hello-world
```
